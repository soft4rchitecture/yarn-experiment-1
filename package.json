{
  "name": "experiment1",
  "version": "1.0.0",
  "description": "root module for this multi module project",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "license": "MIT",
  "scripts": {
    "start": "yarn --cwd backend start",
    "build": "yarn --cwd frontend build"
  }
}
